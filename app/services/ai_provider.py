def generate_reply(message):
    """
    Temporary AI provider interface.
    We will connect the real AI model here next.
    """

    return {
        "reply": "AI provider connection is ready to be configured.",
        "mode": "provider"
    }